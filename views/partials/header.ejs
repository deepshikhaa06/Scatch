<!doctype html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
  <nav class="w-full flex justify-between px-5 py-3">
    <h1 class="text-xl">Scatch</h1>
    <% var loggedin = typeof loggedin !== "undefined" ? loggedin : true; %>
    <% if(loggedin){ %>
      <div class="flex gap-5">
        <a href="/owners/admin" class="text-blue-500 hover:text-blue-700">Add Product</a>
        <a href="/shop" class="text-blue-500 hover:text-blue-700">Shop</a>
        <a href="/cart" class="text-blue-500 hover:text-blue-700">Cart</a>
        <a href="/profile" class="text-blue-500 hover:text-blue-700">My Account</a>
        <a class="text-red-600" href="/users/logout" >Logout</a>
      </div>
    <% } %>
  </nav>
</body>
</html>
